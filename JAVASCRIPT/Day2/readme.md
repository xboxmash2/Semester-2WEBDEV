## Practice questions of for loop

# iterate all the element and print the elements
# add 5 to each number inside the array take array length 5
# Reverse a array the elements should be numeric
# sort the array in descending order
# find the duplicate element and return the unique array


# interview preperation uestions